#include "main.h"

/**
 * exit_shell - Terminates the shell.
 *
 * Releases resources associated with the shell and terminates
 * the current process successfully.
 */

void exit_shell(void)
{
	exit(0);
}
